"""somax simulation CLI (cyclopts-based).

This subpackage hosts the ``somax-sim`` command-line interface and the
runtime support code that backs it: the :class:`RunSpec` dataclass, the
test-case adapter registry, and the core ``simulate`` / ``spinup`` /
``restart`` functions.
"""
